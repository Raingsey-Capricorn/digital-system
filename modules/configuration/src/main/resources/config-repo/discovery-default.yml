server:
  port: 9999
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}
spring:
  security:
    user:
      name: admin
      password: admin@123